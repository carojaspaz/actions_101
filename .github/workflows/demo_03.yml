name: github-actions-demo-03
run-name: ${{ github.actor }} Testing Environment variables 🍏
on: workflow_dispatch
env:
  USER_NAME: KARLA ROJAS

jobs:
  Using-Variables:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello"
        run: echo "$Greeting $USER_NAME welcome to $Repo_Name !"
        env:
          Repo_Name: ${{ github.repository }}
      - run: echo "### IT WORKS! :rocket:" >> $GITHUB_STEP_SUMMARY
  Using-Env-Variables:
    runs-on: ubuntu-latest
    environment: ${{ vars.ENV_DEMO }}
    env:
      Greeting: Hello
      ENV_USER_NAME: ${{ vars.USER_NAME}}
    steps:
      - name: "Say Hello"
        run: echo "$Greeting $ENV_USER_NAME welcome to $Repo_Name !"
        env:
          Repo_Name: ${{ github.repository }}
      - run: echo "### IT WORKS! :rocket:" >> $GITHUB_STEP_SUMMARY
